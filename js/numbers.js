let base = prompt('Add number');
base = Number(base);
console.log(base);

let power = prompt('Add power');
power = Number(power);
console.log(power);

const result = base ** power;
console.log(result);